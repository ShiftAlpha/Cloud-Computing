@model IEnumerable<DomingoRoofWorks.JOB>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@*p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.JOB_CARD_NO)*@
            <text>Job Card Number</text>
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.JOB_CARD_NO)
        </td>
        
        <td>
            @Html.ActionLink("Generate Invoice", "InvoiceDetails", new { id=item.JOB_CARD_NO })
        </td>
    </tr>
}

</table>
